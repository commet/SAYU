# Workflow Status - SAYU Project
# BMad Method - Brownfield Track
# Generated by workflow-init

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2026-01-03T09:30:00Z"
project: "SAYU"
project_type: "brownfield"
selected_track: "method"
field_type: "brownfield"
workflow_path: "_bmad/bmm/workflows/workflow-status/paths/method-brownfield.yaml"

workflow_status:
  - phase: 0
    name: "Documentation"
    note: "Prerequisite for brownfield without docs"
    workflows:
      - id: "document-project"
        status: "required"
        agent: "analyst"
        command: "/bmad:bmm:workflows:document-project"
        output: "Comprehensive project documentation"
        purpose: "Understand existing codebase before planning"

  - phase: 1
    name: "Analysis (Optional Discovery)"
    note: "User-selected during workflow-init"
    workflows:
      - id: "brainstorm"
        status: "optional"
        agent: "analyst"
        command: "/bmad:core:workflows:brainstorming"
        note: "Creative exploration and ideation"
        included: true
      - id: "research"
        status: "optional"
        agent: "analyst"
        command: "/bmad:bmm:workflows:research"
        note: "Domain and technical research"
        included: true

  - phase: 2
    name: "Planning"
    workflows:
      - id: "prd"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-prd"
        output: "Product Requirements Document with FRs and NFRs"
      - id: "create-ux-design"
        status: "conditional"
        agent: "ux-designer"
        command: "/bmad:bmm:workflows:create-ux-design"
        note: "Determined after PRD - if UI work needed"

  - phase: 3
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"
        output: "System architecture document"
        note: "Focused solution design from codebase"
      - id: "create-epics-and-stories"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"
        note: "Break down PRD into implementable epics and stories"
      - id: "test-design"
        status: "optional"
        agent: "tea"
        command: "/bmad:bmm:workflows:testarch-test-design"
        output: "System-level testability review"
      - id: "check-implementation-readiness"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:check-implementation-readiness"
        note: "Validates PRD + Architecture + Epics before implementation"

  - phase: 4
    name: "Implementation"
    note: "Tracked separately in sprint-status.yaml after sprint-planning"
    workflows:
      - id: "sprint-planning"
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "Creates sprint plan - subsequent work tracked in sprint-status.yaml"
